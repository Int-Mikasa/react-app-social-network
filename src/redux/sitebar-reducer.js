
const initialState = {
    friendsData: [
        {id: 5, user: "Olag"},
        {id: 7, user: "Ivan"},
        {id: 8, user: "Frodo"},
        {id: 9, user: "Olga"},
    ]
}

const sitebarReducer = (state = initialState, action) => {

    return state
}

export default sitebarReducer;